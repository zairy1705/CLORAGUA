{
  "name": "cloragua",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "mkdir -p dist && cp -f index.html styles.css app.js metadata.json dist/ 2>/dev/null || echo 'Build ready'",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
